import React from "react";
import text from "../App";
import { StyleSheet, Text, View, Button, TextInput } from "react-native";

const Comp = props => {
  return (
    <View>
      <Text>jskdjalkdjasks{props.name}</Text>
    </View>
  );
};

export default Comp;
